import { View, Text } from "react-native";

function AppIcon() {

    return (
        <View style={{ paddingTop: 60}}>
            <Text>앱 아이콘 변경은... 맨 마지막에 손 대는 것으로</Text>
        </View>
    )
}

export default AppIcon;